[project]
name = "fly-in"
version = "0.1.0"
description = "Fly-in 42 project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.2.0",
    "pydocstyle>=6.3.0",
]
